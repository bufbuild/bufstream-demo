{
  "$id": "buf.bufstream.config.v1alpha1.DispatchConfig.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "local_intake_cache": {
      "description": "If the local intake cache should be used, instead of sharding over the cluster.",
      "type": "boolean"
    },
    "local_topart_append": {
      "description": "If calls to TopartAppend should be local or sharded over the cluster.",
      "type": "boolean"
    },
    "local_topart_fetch": {
      "description": "If calls to TopartFetch should be local or sharded over the cluster.",
      "type": "boolean"
    }
  },
  "title": "Dispatch Config",
  "type": "object"
}
